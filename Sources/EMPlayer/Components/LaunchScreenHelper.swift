import SwiftUI

// MARK: - Launch Screen Resources (AssetCatalog-less)

/// A simple Asset-like helper that references colors & images for the LaunchScreen.
/// We add them in a separate Assets.xcassets folder at Resources.
